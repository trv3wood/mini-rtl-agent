{
  "algorithm": "registered datapath with ready-valid handshake and bubble-free buffering",
  "core_function": "AXI4-Stream skid buffer register with configurable data and control fields",
  "granularity": "leaf",
  "interface_signature": "clk, rst, s_axis_tdata, s_axis_tkeep, s_axis_tvalid, s_axis_tlast, s_axis_tid, s_axis_tdest -> s_axis_tready, m_axis_tdata, m_axis_tkeep, m_axis_tvalid, m_axis_tlast, m_axis_tid, m_axis_tdest, m_axis_tuser",
  "keywords": [
    "axi_stream",
    "register",
    "skid_buffer",
    "ready_valid",
    "datapath",
    "configurable_width"
  ],
  "name": "axis_register",
  "project": "rtl",
  "retrieval_text": "AXI4-Stream skid buffer register with configurable data and control fields. Algorithm: registered datapath with ready-valid handshake and bubble-free buffering. Structure: skid_buffer, datapath_registers, ready_valid_interface. Interface: clk, rst, s_axis_tdata, s_axis_tkeep, s_axis_tvalid, s_axis_tlast, s_axis_tid, s_axis_tdest -> s_axis_tready, m_axis_tdata, m_axis_tkeep, m_axis_tvalid, m_axis_tlast, m_axis_tid, m_axis_tdest,",
  "skill_id": "axis_register",
  "structure": [
    "skid_buffer",
    "datapath_registers",
    "ready_valid_interface"
  ]
}
