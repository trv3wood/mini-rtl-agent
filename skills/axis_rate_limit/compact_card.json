{
  "algorithm": "Token bucket or counter-based rate limiting using configurable numerator and denominator inputs",
  "core_function": "AXI4-Stream interface rate limiter controlling throughput via numerator and denominator parameters",
  "granularity": "leaf",
  "interface_signature": "clk, rst, s_axis_tdata, s_axis_tkeep, s_axis_tvalid, s_axis_tlast, s_axis_tid, s_axis_tdest -> s_axis_tready, m_axis_tdata, m_axis_tkeep, m_axis_tvalid, m_axis_tlast, m_axis_tid, m_axis_tdest, m_axis_tuser",
  "keywords": [
    "axi_stream",
    "rate_limiter",
    "throughput_control",
    "ready_valid",
    "datapath"
  ],
  "name": "axis_rate_limit",
  "project": "rtl",
  "retrieval_text": "AXI4-Stream interface rate limiter controlling throughput via numerator and denominator parameters. Algorithm: Token bucket or counter-based rate limiting using configurable numerator and denominator inputs. Structure: registered_axi4_stream_datapath, rate_control_logic. Interface: clk, rst, s_axis_tdata, s_axis_tkeep, s_axis_tvalid, s_axis_tlast, s_axis_tid, s_axis_tdest -> s_axis_tready, m_axis_tdata, m_axis_tkeep,",
  "skill_id": "axis_rate_limit",
  "structure": [
    "registered_axi4_stream_datapath",
    "rate_control_logic"
  ]
}
